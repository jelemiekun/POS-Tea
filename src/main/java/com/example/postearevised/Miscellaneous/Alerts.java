package com.example.postearevised.Miscellaneous;

import javafx.scene.control.Alert;

public class Alerts {
    private static Alert alert;

    /*public static void loginFormAlert(boolean isAccount) {
        alert = new Alert(Alert.AlertType.ERROR);
        alert.setTitle("Error");
        alert.setHeaderText(isAccount ? "Username/Email Error" : "Password Error");
        alert.setContentText(isAccount ? "Please enter your username or email." : "Please enter your password.");
        alert.show();
    }*/
}
